services:
  ikca:
    image: ghcr.io/kronus09/ikca:latest
    container_name: ikca
    ports:
      - "20509:20509"
    volumes:
      - ikca-data:/app/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped

volumes:
  ikca-data:
